// 교수가 된 현우
// 3474

/*
#include <iostream>

int main()
{
  std::ios_base::sync_with_stdio(false);
  std::cin.tie(NULL);
  std::cout.tie(NULL);

  int T, N, cal;
  long long sum = 0;

  std::cin >> T;

  while (T--)
  {
    std::cin >> N;
    cal = 5;
    sum = 0;

    while (N / cal)
    {
      sum += N / cal;
      cal *= 5;
    }
    std::cout << sum << '\n';
  }
  return 0;
}
*/